package com.example.fitnessapp.data

/**
 * Domain Data Class to represent weight units
 * where UNIT represents a generic unit used for machines with 'difficulty' settings, rather than a specific weight.
 * KG represents kilograms, and LB represents pounds.
*/
enum class WeightUnit {
    KG, LB, UNIT
}